@using OpenBeerMenu.Data.Entities

<div class="bg-zinc-800 rounded-lg p-2 my-2 cursor-grab transition duration-150 hover:scale-x-[1.01] hover:scale-y-110">
    <h3 class="text-xl select-none">@_sectionInfo.Name</h3>
</div>

@code
{
    private SectionInfo _sectionInfo;
    
    [CascadingParameter]
    public object Data { get; set; }

    protected override void OnParametersSet()
    {
        if (Data is not SectionInfo info)
            throw new InvalidOperationException("Data passed to a SectionInfo view was not the correct type!");
        
        _sectionInfo = info;
    }
}